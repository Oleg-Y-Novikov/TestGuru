<tr>
  <td><%= question.body %></td>
  <td>
    <%= link_to t('global.show'), question_path(question) %> |
    <%= link_to t('global.edit'), edit_question_path(question) %> |
    <%= link_to t('global.delete'), question_path(question), method: :delete,
                                                                data: { confirm: t('global.confirm') }, class: 'red' %>
  </td>
</tr>
