<% provide(:title, t('page_questions.edit_question')) %>

<h1><%= question_header(@question.test.title) %></h1>

<%= button_to t('page_questions.show_all_questions'), test_path(@question.test), method: :get %>

<%= render 'form' %>
