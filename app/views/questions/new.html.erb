<% provide(:title, t('page_questions.create_question')) %>

<h1><%= question_header(@test.title) %></h1>
<%= button_to t('page_questions.show_all_questions'), test_path(@test), method: :get %>

<%= render 'form' %>
