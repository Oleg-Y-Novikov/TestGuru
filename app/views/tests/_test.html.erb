<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.category.title %></td>
  <td><%= test.questions.size %></td>

  <td><%= link_to t('global.show'), test_path(test) %> |</td>
  <td><%= link_to t('global.edit'), edit_test_path(test) %> |</td>
  <td><%= link_to t('global.delete'), test_path(test), method: :delete,
                                          data: { confirm: t('global.confirm') }%></td>
</tr>
