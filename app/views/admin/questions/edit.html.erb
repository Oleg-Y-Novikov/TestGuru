<div class="center">
  <h1><%= question_header(@question.test.title) %></h1>
  <%= button_to t('admin.questions.show_all_questions'), admin_test_path(@question.test),
                                                        method: :get, class: 'btn btn-primary' %>
</div>

<%= render 'form' %>
